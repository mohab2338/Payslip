package com.example.salary_tracker

import io.flutter.embedding.android.FlutterFragmentActivity

// local_auth (fingerprint/biometric) requires a FragmentActivity host,
// so this extends FlutterFragmentActivity rather than the default
// FlutterActivity.
class MainActivity : FlutterFragmentActivity()
